package mempool

import (
   //"fmt"
   "testing"
   //"unsafe"
)

func Test_item_alloc_free(t *testing.T) {
/*   mp := NewPool(1024 * 1024, 1.2, false)
   key := "Hello"
   value := "world"
   it := mp.ItemAlloc(key, len(value))
   mp.ItemFree(it)*/
}


